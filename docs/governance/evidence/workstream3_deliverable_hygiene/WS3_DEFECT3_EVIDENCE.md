# WS3 Defect 3 Evidence — inventor-facing hygiene at the canonical merged tip

Every scan below uses the canonical field-aware scanner and exemption
discipline of `tests/test_deliverable_hygiene.py` (21 Prohibited Tokens with
machine-token boundaries; exemption only for enumerated paths whose value is
provably byte-derived from the journey's inventor input; `stated`/`recorded`
prohibited as machine-status field values). Artifacts were generated by
`generate_ws3_artifacts.py` from the real committed routes and
`assemble_deliverable`; nothing was hand-edited.

## Per-journey record

| Journey | Inputs (summary) | §12 issue_type | §12 reference_id | §12 suggested_provider | JSON token hits | HTML token hits | machine-status hits |
|---|---|---|---|---|---|---|---|
| `ws1_completed` | WS1 idea + the byte-identical WS1 answer sequence (carrier sentence, danger statements at iterations 3/4/6/7, unknown at 5) to the true completion branch | `owner_unvalidated` | `rec_1` (unchanged record id) | `You (the inventor)` | 0 | none | 0 |
| `no_answer` | WS1 idea only; no owner response of any kind | `open_gap` | `Mechanism Completeness` | `Not yet determined` | 0 | none | 0 |
| `unknown_action` | WS1 idea + one structured `unknown` action ("I don't know yet.") | `open_gap` | `Mechanism Completeness` | `Not yet determined` | 0 | none | 0 |
| `safety_signal` | PR #122 positive idea + the verbatim inventor safety statement | `owner_unvalidated` | `rec_1` | `You (the inventor)` | 0 | none | 0 |
| `legacy_provenance` | WS1 idea + one accepted-reasoning answer (evidence recorded with the legacy default provenance) | `owner_unvalidated` | `rec_1` | `You (the inventor)` | 0 | none | 0 |

The rendered HTML Suggested-provider field values match the JSON values exactly
(asserted byte-exact by the canonical F1 tests `test_defect3_section12_open_gap_exact_public_values`
and `test_defect3_section12_owner_input_public_value`, both passing).

## All six open-gap types (direct real-assembler states, one open gap each)

| Raw gap type (internal only) | §12 reference_id (exported) | §12 suggested_provider (exported) |
|---|---|---|
| PHYSICAL_FEASIBILITY | `Physical Feasibility` | `Not yet determined` |
| BOUNDARY_AMBIGUITY | `Boundary and Scope` | `Not yet determined` |
| MECHANISM_COMPLETENESS | `Mechanism Completeness` | `Not yet determined` |
| PROBLEM_MECHANISM_FIT | `Problem–Mechanism Fit` | `Not yet determined` |
| ASSUMPTION_INVENTORY | `Assumption Inventory` | `Not yet determined` |
| EXPERTISE_GAP_AWARENESS | `Expertise-Gap Awareness` | `Not yet determined` |

## Legacy provenance

In `legacy_provenance_deliverable.json` (and `ws1_completed_deliverable.json`)
every `evidence_registry[*].provenance` value is the inventor-facing
`Not recorded (pre-provenance session)` — the raw `LEGACY_UNSPECIFIED` token is
absent from every artifact.

## Inventor verbatim text unchanged

- `safety_signal_deliverable.json`: the Safety-Signal `statement` excerpt is
  byte-derived from the inventor's verbatim input statement (substring-verified).
- `ws1_completed_deliverable.json`: `section_2` known-problem/mechanism `content`
  is the inventor's verbatim accepted answer text (byte-derived; substring-verified).
- Acknowledged-unknown verbatim handling is proven by the passing amended
  registry tests (`tests/test_unknown_registry_phase3b1.py`: `content == UNK1`
  verbatim with the public `gap_context` label; A15–A17). Honest observation:
  in the generated `no_answer`/`unknown_action`/`ws1_completed` journey states
  the committed engine records no acknowledged unknown into `unknown_registry`
  (the registry is empty there), so those artifacts exercise the empty-registry
  path; the populated-registry hygiene is covered by the passing existing tests.

## Conclusion (bounded)

At the canonical merged tip, none of the 21 Prohibited Tokens and no raw
`stated`/`recorded` machine-status value appears in any non-exempt value of any
generated inventor-facing JSON artifact or anywhere in any generated HTML
artifact. This record proves exactly what the artifacts and the cited passing
tests show — nothing more.
