# WS3 Defect 4 Evidence — count basis and relationship (machine-checkable)

Source artifact: `ws1_completed_deliverable.json` (generated by
`generate_ws3_artifacts.py` at the canonical merged tip; not hand-edited).

## Section 4

- `count_basis`: `evidence_derived_requirements`
- `total`: 8
- actual requirement count (`len(requirements)`): 8

## Section 13

- `count_basis`: `requirement_landscape`
- `total`: 13
- actual requirement count (`len(requirements)`): 13
- complete `count_relationship` (verbatim from the artifact):

```json
{
  "broader_set": "requirement_landscape",
  "narrower_view": "evidence_derived_requirements",
  "relationship": "evidence_derived_requirements is a narrower, evidence-oriented view; it is not the same collection as requirement_landscape and the two totals are counted from different sets."
}
```

## Section 14

- `validation_plan_source`: `requirement_landscape`
- `validation_step_total`: 13
- actual step count (`len(steps)`): 13
- `blocked_item_total`: 0
- actual blocked count (`len(blocked_items)`): 0

## Bound arithmetic

`validation_step_total + blocked_item_total == Section 13 total`
-> `13 + 0 == 13` -> **True**

## No artificial §4/§13 equality

`Section 4 total (8) != Section 13 total (13)` — the two sets remain distinct
collections with distinct bound count bases; nothing forces them equal. The
same properties are continuously asserted by the passing canonical tests
(`test_defect4_*`, 7 tests in `tests/test_deliverable_hygiene.py`).
