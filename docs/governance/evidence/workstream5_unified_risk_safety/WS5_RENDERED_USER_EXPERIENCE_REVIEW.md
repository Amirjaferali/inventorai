# WS5_RENDERED_USER_EXPERIENCE_REVIEW — the three deterministic cases at HEAD

All excerpts below are verbatim from the normalized HEAD artifacts in this
directory (regenerated by `generate_ws5_artifacts.py head <thisdir>` at
`97b6725953150509059dd41ba623e438f939f094`). Inputs are deterministic and
committed; nothing was fabricated.

## Case A — WS1 safety-bearing journey

- **Input identity:** the byte-identical committed Workstream 1 baseline
  journey (idea + answers + one unknown action, verbatim constants in the
  harness; identical to `docs/governance/evidence/workstream1_deliverable_baseline/inputs_false_negative_journey.json`).
- **Safety signals:** 3 (subjects `dangerous`/`danger`/`fire`).
- **Section 6:** 1 system-derived row — `[low] Evidence is reasoned
  (substantiated but not demonstrated).`
- **Section 13:** `risks: []`; frozen disclaimer rendered.
- **Linkage object:** `signals_present: true`, `signal_total: 3`,
  `section_6_risk_total: 1` (low 1), `section_6_note` + `section_13_note`
  populated, `section_6_empty_qualification: null` (rows exist).
- **Inventor-facing output (Section 6 region, `head_ws1_safety_deliverable.html`):**
  the `[low]` row followed by: "You also described possible safety
  consequences in your own words. See “Inventor-Stated Safety Signals.”
  These are your statements, not confirmed risks, and they still require
  independent validation."
- **Section 13 vicinity:** the unchanged disclaimer immediately followed by:
  "You also described possible safety consequences in your own words. They
  are listed separately under “Inventor-Stated Safety Signals.” They are not
  structural risk records and have not been independently validated."
- **Why truthful and non-misleading:** the low process row is neither
  deleted nor re-graded; the inventor's stated dangers are pointed to at the
  exact place a reader looks for "what could go wrong," labeled as their own
  unvalidated statements; the structural-risk negation is reconciled rather
  than left to read as "no risks."

## Case B — Mature/gap-free signal-bearing state

- **Input identity:** a real session on the WS1 idea with `maturity_level=2`,
  zero gaps, demonstrated-quality evidence, and one ledger record carrying
  the committed PR #122 positive statement verbatim ("If insulation cannot
  be safely achieved inside the plug housing, the device should not be used
  because it could create a safety risk.").
- **Safety signals:** 1. **Section 6:** 0 rows. **Section 13:** `risks: []`.
- **Linkage object:** all three wordings populated (empty-qualification
  active because Section 6 has zero rows while a signal exists).
- **Inventor-facing output (Section 6 region, `head_mature_gapfree_deliverable.html`):**
  instead of the bare "No risks recorded.": "No system-derived risks were
  identified from the current session state. This does not mean the idea is
  safe or risk-free. Safety consequences you described are listed separately
  under “Inventor-Stated Safety Signals” and have not been independently
  validated."
- **Why truthful and non-misleading:** the strongest misleading case at BASE
  (an empty risk section beside a stated safety consequence) now actively
  denies the "safe / risk-free" implication and routes the reader to their
  own statement without promoting it to a risk record.

## Case C — No-signal fresh session

- **Input identity:** `/start` with the WS1 idea text only; no responses.
- **Safety signals:** 0. **Section 6:** 2 system-derived rows (assessment
  completeness `[high]`, unresolved gap `[medium]`).
- **Linkage object:** `signals_present: false`; every wording field `null`.
- **Inventor-facing output:** byte-identical to the pre-Workstream-5
  rendering — no Workstream 5 wording anywhere
  (`head_no_signal_deliverable.html` carries none of the three notes; the
  frozen disclaimers render alone).
- **Why truthful and non-misleading:** with nothing inventor-stated to link,
  nothing is invented; the existing truthful presentation is preserved
  unchanged.
