# F-010 — Scoring/Normalizer Contract Divergence (Diagnostic-Only)

classification: TEST EVIDENCE + ANALYSIS
finding_id: F-010
date: 2026-05-25
session: Phase K
status: CONTAINED — no production fix required

## Title
c6_gate_utility requires recommendation.action which normalize_output never produces.
Defect is confined to diagnostic replay runner v2. Production is not affected.

## Evidence

### 1. score_case call sites
./engine/scoring.py:90:def score_case(normalized_output, testcase):
./scripts/run_replay_benchmark_v2.py:67: actual = score_case(norm, tc)
Total callers: 1.

### 2. normalize_output.py
grep recommendation engine/normalize_output.py: NOT FOUND
File is 27 lines. Transforms existing keys only. Never constructs recommendation.

### 3. extract_json.py
grep recommendation engine/extract_json.py: NOT FOUND

### 4. web/app.py
grep score_case|normalize_output|extract_json web/app.py: NOT FOUND

### 5. c6_gate_utility
engine/scoring.py:55: def c6_gate_utility(out, tc):
engine/scoring.py:57: rec = out.get("recommendation", {})
engine/scoring.py:58: act = rec.get("action", "")
engine/scoring.py:59: if not act: iss.append("MISSING:recommendation.action"); return False, iss

## Impact
Production: None proven.
v1 replay: None. v1 does not call score_case.
v2 replay: Affected. TC-A4 UNTESTABLE via REQUIRED_CONTRACTS.

## Decision
CONTAINED. No production fix required.
