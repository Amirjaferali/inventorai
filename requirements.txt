# G-IRB Implementation-Readiness Baseline — pinned runtime + test dependencies.
#
# Only dependencies proven necessary from actual imports are pinned here. A scan of
# web/ engine/ tests/ for non-stdlib top-level imports yields exactly three third-party
# packages:
#   Flask      — imported by web/app.py (the thin Flask web shell)
#   pytest     — the test runner for the tests/ suite
#   weasyprint — imported LAZILY by web/app.py inside the private PDF-renderer
#                helper (never at module import time), so a missing native
#                library affects only the Direct Output PDF endpoint and never
#                prevents the HTML application from starting. It renders the
#                deliverable synchronously in memory from a trusted PDF-only
#                document string, with a deny-all URL fetcher: no network, no
#                filesystem output, no temporary file, no persistence.
# Everything else imported by the codebase is in the Python standard library.
#
# INFRA-G1-R2 (the bounded implementation under the authoritative INFRA-G1-R1
# contract) adds ONE further pinned package that is NOT imported by the
# codebase but is required to SERVE it in production:
#   gunicorn — the production WSGI server (see `gunicorn.conf.py`: exactly one
#              worker and one thread, preserving the single thread-bound SQLite
#              connection). It is provider-neutral and is never used by the
#              development path (`web/app.py _run_config`), which is unchanged.
#
# Versions are pinned to the exact set proven to install and run the governed suite
# green in a clean isolated environment (Python 3.11). Transitive dependencies
# (Werkzeug, Jinja2, click, itsdangerous, MarkupSafe, iniconfig, pluggy, packaging)
# are resolved by pip; add a constraints.txt only if stricter transitive pinning is
# later shown to be genuinely required.
#
# `weasyprint` needs the system Pango/HarfBuzz/Fontconfig stack and a font that
# covers the rendered scripts (DejaVu Sans is used for Latin and Arabic). Those
# are OS packages, not pip packages, and are therefore not pinned here.
Flask==3.1.3
gunicorn==26.1.0
pytest==9.1.1
weasyprint==70.0
