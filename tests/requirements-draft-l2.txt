# Draft Level 2 — Same-Device Unsubmitted-Text Recovery: TEST-ONLY dependencies.
#
# Authorized by G-DRAFT-L2-LOCAL-CONTINUITY-IMPLEMENTATION-01 (§16) and its
# remediation G-DRAFT-L2-LOCAL-CONTINUITY-REMEDIATION-01. These are NOT runtime/
# production dependencies (the app ships no client framework and the production
# `requirements.txt` stays Flask + pytest only). They are required ONLY to run
# tests/test_draft_l2_local_continuity.py, because proving client-side
# localStorage persistence across reload/offline/close/multi-tab needs a real
# browser — server-only Flask tests cannot.
#
# Browser executable expectation: the Chromium binary is pre-provisioned in the
# execution environment (PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers); no browser
# download is needed (PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1). The tests launch it via
# `executable_path` (globbed from /opt/pw-browsers/chromium-*/chrome-linux/chrome)
# or the Playwright default. No frontend framework / Jest / npm app deps.
#
# By default a missing `playwright` package SKIPS the browser tests (so the full
# governed suite stays green where the browser is unavailable). To make the
# DEDICATED Draft L2 run FAIL LOUDLY instead of silently skipping when the
# dependency/browser is missing, set INVENTORAI_DRAFT_L2_REQUIRE_BROWSER=1.
#
# Pinned to the versions proven green in this environment.
playwright==1.62.0
pytest-playwright==0.8.0
